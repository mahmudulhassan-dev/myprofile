# Project Structure & Documentation

এই প্রজেক্টটি একটি **Full-Stack React & Node.js Website**। নিচে প্রতিটি ফোল্ডার এবং ফাইলের বিস্তারিত বিবরণ দেওয়া হলো।

## 📂 Root Directory (`/myprofile`)
- **`package.json`**: প্রজেক্টের সকল ডিপেন্ডেন্সি এবং স্ক্রিপ্ট (যেমন `dev:full` যা ফ্রন্টএন্ড ও ব্যাকএন্ড একসাথে চালায়) এখানে থাকে।
- **`vite.config.js`**: Vite কনফিগারেশন ফাইল, যেখানে পোর্ট সেট করা এবং প্রক্সি কনফিগার করা থাকে।
- **`tailwind.config.js`**: Tailwind CSS এর কনফিগারেশন, যেখানে কালার প্যালেট, ফন্ট এবং থিম কাস্টমাইজ করা হয়েছে।
- **`postcss.config.js`**: CSS প্রসেসিং কনফিগারেশন।
- **`index.html`**: মেইন HTML ফাইল যা ব্রাউজারে লোড হয়।

## 📂 Backend (`/server`)
ব্যাকএন্ডের সকল লজিক এবং ডাটাবেস কানেকশন এখানে থাকে।
- **`index.js`**: মেইন সার্ভার ফাইল। এখানে Express অ্যাপ তৈরি, API রুট ডিফাইন এবং ডাটাবেস সিঙ্ক করা হয়।
- **`/config`**:
    - **`db.js`**: MySQL ডাটাবেস কানেকশন সেটআপ (Sequelize ব্যবহার করে)।
- **`/models`**: ডাটাবেস টেবিলগুলোর মডেল ডেফিনিশন।
    - **`index.js`**: সকল মডেল (`Profile`, `Project`, `User`, `Pricing`, `FAQ` ইত্যাদি) এখানে ডিফাইন এবং এক্সপোর্ট করা হয়েছে।

## 📂 Frontend (`/src`)
রিয়্যাক্ট ফ্রন্টএন্ডের সকল কোড।
- **`App.jsx`**: মেইন অ্যাপ্লিকেশন কম্পোনেন্ট। রাউটিং (Admin vs Public) এখানে হ্যান্ডেল করা হয়।
- **`main.jsx`**: অ্যাপটি মাউন্ট করার এন্ট্রি পয়েন্ট।
- **`index.css`**: গ্লোবাল স্টাইল এবং Tailwind ইম্পোর্ট।

### 📂 Components (`/src/components`)
সকল রিইউজেবল UI কম্পোনেন্ট।
- **`Navbar.jsx`**: উপরের নেভিগেশন বার, যা স্ক্রল করলে স্টাইল চেঞ্জ করে এবং রেসপন্সিভ।
- **`Hero.jsx`**: ওয়েবসাইটের প্রথম সেকশন (ব্যানার), যা ডাটাবেস থেকে টেক্সট এবং ছবি লোড করে।
- **`Services.jsx`**: সার্ভিস বা সেবাগুলোর কার্ড ডিসপ্লে করে।
- **`Projects.jsx`**: পোর্টফোলিও গ্যালারি এবং ফিল্টারিং সিস্টেম।
- **`Skills.jsx`**: স্কিল বা দক্ষতার প্রগ্রেস বার এবং লিস্ট।
- **`Products.jsx`**: ই-কমার্স বা ডিজিটাল প্রোডাক্ট শোকেস।
- **`Contact.jsx`**: কন্টাক্ট ফর্ম।
- **`Footer.jsx`**: ফুটার সেকশন।
- **`ChatWidget.jsx`**: AI চ্যাটবট উইজেট (Google Gemini দিয়ে চালিত)।
- **`PaymentModal.jsx`**: পেমেন্ট বা অরডার করার জন্য পপআপ মোডাল।

### 📂 Admin Components (`/src/components/admin`)
এডমিন প্যানেলের সকল মডিউল।
- **`AdminSidebar.jsx`**: এডমিন ড্যাশবোর্ডের বাম পাশের মেনু বার।
- **`SettingsPanel.jsx`**: গ্লোবাল সেটিংস (Title, Logo, Colors, Social Links)।
- **`ProfileManager.jsx`**: নিজের নাম, বায়ো, ছবি পরিবর্তন করার মডিউল।
- **`ServicesManager.jsx`**: সার্ভিস অ্যাড/এডিট/ডিলিট করার মডিউল।
- **`ProjectsManager.jsx`**: প্রজেক্ট অ্যাড এবং গ্যালারি ইমেজ আপলোড করার মডিউল।
- **`SkillsManager.jsx`**: স্কিল লেভেল এবং ক্যাটাগরি সেট করার মডিউল।
- **`BlogManager.jsx`**: ব্লগ পোস্ট লিখা এবং পাবলিশ করার মডিউল।
- **`TestimonialsManager.jsx`**: ক্লায়েন্ট রিভিউ ম্যানেজ করার মডিউল।
- **`UsersManager.jsx`**: ইউজার রোল (Admin/Client) এবং স্ট্যাটাস চেঞ্জ করার মডিউল।
- **`SeoManager.jsx`**: মেটা ট্যাগ এবং এসইও সেটিংস মডিউল।
- **`SubscribersManager.jsx`**: নিউজলেটার সাবস্ক্রাইবার লিস্ট এবং CSV এক্সপোর্ট।
- **`PricingManager.jsx`**: প্রাইসিং প্যাকেজ তৈরি করার মডিউল।
- **`FaqManager.jsx`**: প্রশ্ন ও উত্তর (FAQ) সেট করার মডিউল।
- **`OrdersManager.jsx`**: কাস্টমার অর্ডার এবং পেমেন্ট স্ট্যাটাস দেখার মডিউল।
- **`MediaManager.jsx`**: আপলোড করা সকল ছবি দেখার গ্যালারি।

### 📂 Context (`/src/context`)
- **`LanguageContext.jsx`**: মাল্টি-ল্যাঙ্গুয়েজ (English/Bengali) সাপোর্ট হ্যান্ডেল করে।
- **`SettingsContext.jsx`**: পুরো অ্যাপ জুড়ে গ্লোবাল সেটিংস (যেমন সাইটের নাম, কালার) সরবরাহ করে।

### 📂 Data (`/src/data`)
- **`locales.js`**: স্ট্যাটিক টেক্সটের অনুবাদ ফাইল।

## 📂 Public (`/public`)
- **`/uploads`**: ডাইনামিকলি আপলোড করা সকল ছবি এখানে সেভ হয়।
- **`profile.jpg`**: ডিফল্ট ছবি।

---
**Last Updated:** December 05, 2025
**Features Count:** 50+ Modules Active.
